spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: data
  datasource:
    url: jdbc:postgresql://localhost:5777/fitnessDB
    username: postgres
    password: Aries1994
    driverClassName: org.postgresql.Driver

user-service:
  mail-service:
    uri: http://10.0.0.1:8080
